Create a new specialist agent scaffold called `Baggage Agent` in the TypeScript backend.

Scope:
- Add the agent definition and handoff wiring in `typescript-backend/src/airline/agents.ts`.
- Reuse or extend `baggage_tool` in `typescript-backend/src/airline/tools.ts`.
- Keep prompts/instructions aligned with existing agent style.
- Update README sections that list included agents and demo flows.

Constraints:
- Keep changes minimal and consistent with current project patterns.
- Do not break existing flows.
- If adding tests, keep them focused and lightweight.
